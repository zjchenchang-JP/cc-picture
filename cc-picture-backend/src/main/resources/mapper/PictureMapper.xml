<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjcc.ccpicturebackend.mapper.PictureMapper">

    <resultMap id="BaseResultMap" type="com.zjcc.ccpicturebackend.model.entity.Picture">
            <id property="id" column="id" />
            <result property="url" column="url" />
            <result property="name" column="name" />
            <result property="introduction" column="introduction" />
            <result property="category" column="category" />
            <result property="tags" column="tags" />
            <result property="picSize" column="picSize" />
            <result property="picWidth" column="picWidth" />
            <result property="picHeight" column="picHeight" />
            <result property="picScale" column="picScale" />
            <result property="picFormat" column="picFormat" />
            <result property="userId" column="userId" />
            <result property="createTime" column="createTime" />
            <result property="editTime" column="editTime" />
            <result property="updateTime" column="updateTime" />
            <result property="isDelete" column="isDelete" />
            <result property="thumbnailUrl" column="thumbnailUrl" jdbcType="VARCHAR"/>
            <result property="spaceId" column="spaceId" />
            <result property="picColor" column="picColor" />
    </resultMap>

    <sql id="Base_Column_List">
        id,url,thumbnailUrl,name,spaceId,picColor,introduction,category,tags,
        picSize,picWidth,picHeight,picScale,picFormat,
        userId,createTime,editTime,updateTime,isDelete
    </sql>

    <select id="getCategoryStatistics" resultType="map">
        SELECT category AS category,
        COUNT(*) AS count,
        SUM(picSize) AS totalSize
        FROM picture
        WHERE isDelete = 0
        <if test="params.queryAll != null and params.queryAll">
        </if>
        <if test="params.queryPublic != null and params.queryPublic">
            AND spaceId IS NULL
        </if>
        <if test="params.spaceId != null">
            AND spaceId = #{params.spaceId}
        </if>
        GROUP BY category;
    </select>
</mapper>
